# Normalize line endings to LF in the repo. The generator runs on ubuntu-latest,
# and generated seed files are committed from there — keep everything LF.
* text=auto eol=lf
*.png binary
*.jpg binary
